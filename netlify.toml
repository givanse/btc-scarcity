# https://docs.netlify.com/configure-builds/file-based-configuration/#sample-file

[build]
  publish = "build"
  functions = "functions"
  command = "yarn install && yarn build && cd functions/btc-usd && yarn install"

[context.production.environment]
  NODE_ENV = "production"
